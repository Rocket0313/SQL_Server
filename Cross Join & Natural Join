Cross Join : A CROSS JOIN is a type of join that returns the Cartesian product of the two tables involved in the join.

select * from emp,dept;
or 
select * from emp
cross join dept;

Table a has 10 rows              table b has 5 rows  
Corss join 10 x 5 => 50 records 


Natural Join : A NATURAL JOIN is a type of SQL join that automatically joins two tables based on all columns with the same name and compatible data types.

SELECT *
FROM departments
NATURAL JOIN employees;
